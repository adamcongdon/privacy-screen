{
  "name": "privacy-screen",
  "version": "1.0.0",
  "description": "PII anonymization hook for Claude Code — keeps customer data local",
  "type": "module",
  "scripts": {
    "test": "bun test tests/",
    "cli": "bun cli/PrivacyScreen.ts",
    "hook": "bun hooks/PrivacyScreen.hook.ts",
    "lint": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
