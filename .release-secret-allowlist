# Known-safe ship exceptions for the release secret/PII scan gate (scripts/build-release.ts).
# Lines starting with "/" are path allowlist entries; everything else is a value entry.

# Bun toolchain provenance paths: every official Bun darwin binary is built on
# Buildkite macOS agents under /Users/administrator, and `bun build --compile`
# embeds that runtime — so these strings ship in ALL our darwin artifacts.
# Verified 2026-07-22: `strings $(which bun) | grep -c /Users/administrator` → 17,
# all under .../buildkite-agent/builds/.../bun/vendor/. Not user data.
/Users/administrator/Library/Services/buildkite-agent
