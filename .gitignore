node_modules/

# SQLite database — contains real PII (customer names, IPs, etc.) — NEVER commit
*.db
*.db-shm
*.db-wal

# Your personal config with actual customer names
PRIVACY_CONFIG.yaml

# Bun lockfile (optional — commit if you want reproducible installs)
# bun.lockb
